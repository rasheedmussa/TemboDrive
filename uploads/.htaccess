# Deny access to PHP files in uploads to prevent execution
<Files *.php>
    Order deny,allow
    Deny from all
</Files>

# Also deny other executable files
<FilesMatch "\.(php|pl|py|jsp|asp|exe|scr|dll)$">
    Order deny,allow
    Deny from all
</FilesMatch>